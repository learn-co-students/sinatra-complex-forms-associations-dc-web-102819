<h1>Update Owner</h1>
 
<h2>Edits for <%= @pet.name %></h2>
 
  <form action="/owners/<%= @pet.id %>" method="POST">
    <input id="hidden" type="hidden" name="_method" value="patch">
    <label>Edit the Owner's Name:</label>
    <br>
    <input type="text" name="owner[name]" value="<%= @pet.name %>">
 
    <br>
    <p>Select an existing human or create a new one below.</p>
 
 
    <h3>Existing Owners</h3>
    <% @owners.each do |owner| %>
      <input type="checkbox" name="pet[owner_ids][]" id="<%= @owner.id %>" value="<%= @pet.id %>" <%='checked' if @pet.owners.include?(owner) %>><%= owner.name %></input><br>
    <% end %>
    <br>
 
    <h3>New Owner</h3>
    <label>Owner Name: </label>
    <br>
    <input type="text" name="owner[name]" id="owner_name"></input>
    <br><br>
 
    <input type="submit" value="Update Pet">
  </form>